import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container:{
        width: "80%",
        marginVertical: 10
    },
    gradient:{
        width: 240,
        padding: 15,
        borderRadius: 8,
        alignItems: "center",
        flexDirection: "row",
        gap: 10,
        justifyContent: "center"
    },
    title:{
        fontSize: 14,
        color: "#F2f4f6",
        textAlign: "center"
    }
})